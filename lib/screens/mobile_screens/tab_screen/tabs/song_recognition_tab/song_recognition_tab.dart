import 'package:flutter/material.dart';

class SongRecognitionTab extends StatelessWidget {
  const SongRecognitionTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
